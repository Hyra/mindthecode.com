- var pageCount = Math.floor(getPageCount());
  - var prev = parseInt(page) + 1;
  - var next = parseInt(page) - 1;
  script.
    //- alert(#{pageCount});
    //- alert(#{page});
  if pageCount > 1
    ul.pagination
      if page < pageCount - 1
        a.older(href=pageURL(prev)) «&nbsp;Older posts
      if page > 0
        if next == 0
          a.newer(href='/') Newer&nbsp;»
        else
          a.newer(href=pageURL(page-1)) Newer&nbsp;» 
